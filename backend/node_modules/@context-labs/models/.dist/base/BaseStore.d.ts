import LogManager from './interfaces/LogManager';
export interface BaseStoreParams {
    storeName: string;
}
export default class BaseStore {
    storeName: string;
    logger: LogManager;
    constructor(params: BaseStoreParams);
}
