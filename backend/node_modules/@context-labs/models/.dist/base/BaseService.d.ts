import LogManager from './interfaces/LogManager';
export interface BaseServiceParams {
    serviceName: string;
}
export default class BaseService {
    serviceName: string;
    logger: LogManager;
    constructor(params: BaseServiceParams);
}
