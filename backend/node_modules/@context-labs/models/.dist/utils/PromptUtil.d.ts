import { PromptConfiguration } from '../services/BotService';
import { Snippet } from '../services/SnippetService';
declare const PromptUtil: {
    createHydePrompt: (query: string, entityName: string) => string;
    createContextPrompt: (botTitle: string, promptConfiguration: PromptConfiguration | null, query: string, snippets: Snippet[]) => string;
    fixMarkdownPrompt: (markdown: string) => string;
    fixUnstructuredMarkdownPrompt: (markdown: string) => string;
};
export default PromptUtil;
