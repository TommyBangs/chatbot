import { Snippet } from '../services/SnippetService';
declare const SnippetEdgeUtil: {
    link: (snippet: Snippet, utms?: string[]) => string;
    addUTMs: (url: string, utms?: string[]) => string;
};
export default SnippetEdgeUtil;
