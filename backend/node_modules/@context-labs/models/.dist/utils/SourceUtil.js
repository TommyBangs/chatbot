"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fileIds = (source) => {
    var _a;
    return [(_a = source === null || source === void 0 ? void 0 : source.imageFileId) !== null && _a !== void 0 ? _a : ''];
};
const durationHours = (source) => {
    var _a;
    return parseFloat(String(((_a = source === null || source === void 0 ? void 0 : source.durationSeconds) !== null && _a !== void 0 ? _a : 0) / 3600)).toFixed(1);
};
const documentCount = (sources) => {
    return sources.map((source) => source.documentCount).reduce((a, b) => a + b, 0);
};
const SourceUtil = {
    fileIds,
    durationHours,
    documentCount,
};
exports.default = SourceUtil;
//# sourceMappingURL=SourceUtil.js.map