"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const isQueued = (sync) => {
    if (!sync)
        return false;
    if (sync.info.total === 0)
        return true;
    return false;
};
const isRunning = (sync) => {
    if (!sync)
        return false;
    if (sync.info.total === 0)
        return true;
    return sync.info.completed + sync.info.failed < sync.info.total;
};
const isSyncing = (sync) => {
    return isQueued(sync) || isRunning(sync);
};
const SyncUtil = {
    isQueued,
    isRunning,
    isSyncing,
};
exports.default = SyncUtil;
//# sourceMappingURL=SyncUtil.js.map