import type { videoInfo } from 'ytdl-core';
import ytpl from 'ytpl';
/**
 *
 */
declare const YouTubeUtil: {
    videoViews: (info: videoInfo) => number;
    sourceDurationSeconds: (items: ytpl.Item[]) => number;
    sourceDurationHours: (items: ytpl.Item[]) => number;
    sourceTranscriptionCost: (items: ytpl.Item[]) => number;
};
export default YouTubeUtil;
