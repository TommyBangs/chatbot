import { Source } from '../services/SourceService';
declare const SourceUtil: {
    fileIds: (source: Source) => string[];
    durationHours: (source: Source) => string;
    documentCount: (sources: Source[]) => number;
};
export default SourceUtil;
