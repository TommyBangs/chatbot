"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ConsoleLogManager_1 = __importDefault(require("../base/ConsoleLogManager"));
const logger = new ConsoleLogManager_1.default({ name: 'ZodUtil' });
function convertKebabCaseToTitleCase(input) {
    const words = input.split('-');
    const capitalizedWords = words.map((word) => word.charAt(0).toUpperCase() + word.slice(1));
    return capitalizedWords.join(' ');
}
const parseSchema = (schema, object) => {
    const result = schema.safeParse(object);
    if (result.success === false) {
        let message = '';
        logger.error('Schema parse error', result.error.message);
        result.error.issues.forEach((issue) => {
            const key = issue.path[issue.path.length - 1];
            message += `${convertKebabCaseToTitleCase(key)}: ${issue.message},`;
        });
        return {
            success: false,
            error: message,
        };
    }
    return {
        success: true,
    };
};
const ZodUtil = {
    parseSchema,
};
exports.default = ZodUtil;
//# sourceMappingURL=ZodUtil.js.map