import slug from 'slug';
declare const StringUtil: {
    capitalize: (value: string) => string;
    pluralize: (singular: string, plural: string, count: number) => string;
    lowerCaseTrim: (value: string) => string;
    slug: typeof slug;
    unmaskNumber: (maskedValue: string) => number;
    condenseNumber: (number: string | number, digits?: number) => string;
    addNumberOrdinals: (n: number) => string;
};
export default StringUtil;
