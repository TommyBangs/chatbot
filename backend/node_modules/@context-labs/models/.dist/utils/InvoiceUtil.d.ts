import { UpdateUsage, Usage } from '../interfaces/Usage';
import { Invoice } from '../services/InvoiceService';
export type InvoiceCheck = {
    hasCredits: boolean;
    hasDocuments: boolean;
    hasSyncs: boolean;
};
declare const InvoiceUtil: {
    checkInvoice: (invoice: Invoice | null) => InvoiceCheck;
    changeReport: (invoice: Invoice | null, usageDiff: UpdateUsage) => InvoiceCheck;
    addUsage: (current: Usage, addend: UpdateUsage) => Usage;
    usagePercentage: (invoice: Invoice | null) => Usage | null;
};
export default InvoiceUtil;
