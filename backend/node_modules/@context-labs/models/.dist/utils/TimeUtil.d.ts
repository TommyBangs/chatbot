import { DateTime } from 'luxon';
declare const TimeUtil: {
    currentBrowserTimezone: () => string;
    now: (timezone?: string) => number;
    fromNow: (millis: number | string, timezone?: string) => number;
    date: (millis: number | string, timezone?: string) => DateTime;
    format: (millis: number | string, format?: string, timezone?: string) => string;
    ago: (millis: number | string, timezone?: string) => string;
};
export default TimeUtil;
