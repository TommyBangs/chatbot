import { SourceTypes } from '../services/SourceService';
import { Document, DocumentSummary, DocumentTypes } from '../services/DocumentService';
declare const DocumentUtil: {
    convertToSummary: (document: Document) => DocumentSummary;
    typeFromSourceType: (sourceType: SourceTypes) => DocumentTypes;
};
export default DocumentUtil;
