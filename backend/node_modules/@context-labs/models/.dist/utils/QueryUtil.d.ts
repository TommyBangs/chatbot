import { Query, QueryEdit } from '../services/QueryService';
declare const QueryUtil: {
    latestEdit: (query: Query | null) => QueryEdit | null;
    currentValue: (query: Query | null) => string | null;
    typeName: (query: Query | null) => string;
};
export default QueryUtil;
