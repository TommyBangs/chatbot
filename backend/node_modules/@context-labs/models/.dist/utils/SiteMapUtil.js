"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const sitemapper_1 = __importDefault(require("@context-labs/sitemapper"));
const cheerio = __importStar(require("cheerio"));
const getFavicon = (url) => __awaiter(void 0, void 0, void 0, function* () {
    var _a, _b, _c;
    const res = yield fetch(url);
    const html = yield res.text();
    const $ = cheerio.load(html);
    const favicon = (_b = (_a = $('link[rel="shortcut icon"]').prop('href')) !== null && _a !== void 0 ? _a : $('link[rel="icon"]').prop('href')) !== null && _b !== void 0 ? _b : $('link[rel="apple-touch-icon"]').prop('href');
    if ((_c = favicon === null || favicon === void 0 ? void 0 : favicon.includes) === null || _c === void 0 ? void 0 : _c.call(favicon, 'http')) {
        return favicon;
    }
    return `${url}${favicon}`;
});
const parse = (url) => __awaiter(void 0, void 0, void 0, function* () {
    const sitemap = new sitemapper_1.default({
        url,
        timeout: 60000,
    });
    const { sites } = yield sitemap.fetch();
    const { origin, host } = new URL(url);
    const favicon = yield getFavicon(origin);
    return {
        title: host,
        sites: [...new Set(sites)],
        imageUrl: favicon,
    };
});
const SiteMpaUtil = {
    parse,
};
exports.default = SiteMpaUtil;
//# sourceMappingURL=SiteMapUtil.js.map