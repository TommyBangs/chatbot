"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const videoViews = (info) => {
    const views = info.player_response.videoDetails.viewCount;
    return parseInt(views, 10);
};
const sourceDurationSeconds = (items) => {
    var _a, _b;
    return (_b = (_a = items === null || items === void 0 ? void 0 : items.map((video) => {
        var _a;
        return (_a = video === null || video === void 0 ? void 0 : video.durationSec) !== null && _a !== void 0 ? _a : 0;
    })) === null || _a === void 0 ? void 0 : _a.reduce((cur, next) => cur + next, 0)) !== null && _b !== void 0 ? _b : 0;
};
const sourceDurationHours = (items) => {
    const seconds = sourceDurationSeconds(items);
    return seconds > 0 ? Math.ceil(seconds / 3600) : 0;
};
/**
 * Return value is in cents
 */
const sourceTranscriptionCost = (items) => {
    const hours = sourceDurationHours(items);
    return hours * 0.5 * 100;
};
/**
 *
 */
const YouTubeUtil = {
    videoViews,
    sourceDurationSeconds,
    sourceDurationHours,
    sourceTranscriptionCost,
};
exports.default = YouTubeUtil;
//# sourceMappingURL=YouTubeUtil.js.map