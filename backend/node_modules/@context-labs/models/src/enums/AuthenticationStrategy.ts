import z from 'zod';

export const zAuthenticationStrategy = z.enum([
  'BasicRegister',
  'BasicLogin',
  'LinkedIn',
  'Google',
]);

type AuthenticationStrategy = z.infer<typeof zAuthenticationStrategy>;

export default AuthenticationStrategy;
