const emails = [
  'sam@usecontext.io',
  'joel@usecontext.io',
];

const isSuperUser = (email: string): boolean => emails.includes(email);

const SuperUserUtil = {
  isSuperUser,
};

export default SuperUserUtil;
